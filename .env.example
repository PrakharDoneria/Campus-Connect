# Firebase Configuration
# Get these from your Firebase project settings > General > Your apps > Web app
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# MongoDB Configuration
# Get your connection string from MongoDB Atlas
MONGODB_URI=

# Google AI Configuration
# Get your API key from https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=

# Push Notifications (Web-Push VAPID Keys)
# Generate a VAPID key pair using: npx web-push generate-vapid-keys
# IMPORTANT: These must be a matching pair generated together
# The public key should be URL-safe base64 encoded
NEXT_PUBLIC_FIREBASE_VAPID_KEY=your_vapid_public_key_here
VAPID_PRIVATE_KEY=your_vapid_private_key_here

# Note: Despite the name "FIREBASE_VAPID_KEY", this project uses standard Web-Push
# with VAPID keys, NOT Firebase Cloud Messaging. The name is kept for backwards compatibility.
